<% title 'Likes' %>

<div class="page-grid likes">
  <% @user_pages.each do |user_page| %>
    <%= render partial: 'user_pages/item', locals: { title_url: like_path(user_page), user_page: user_page } %>
  <% end %>
</div>

<%= will_paginate @user_pages %>
