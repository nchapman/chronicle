<%= form_for(@save) do |f| %>
  <% if @save.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@save.errors.count, "error") %> prohibited this save from being saved:</h2>

      <ul>
      <% @save.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
