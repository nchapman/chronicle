<% title 'Reading List' %>

<div class="page-grid likes">
  <% @user_pages.each do |user_page| %>
    <%= render partial: 'user_pages/item', locals: { title_url: save_path(user_page), user_page: user_page } %>
  <% end %>
</div>

<%= will_paginate @user_pages %>
